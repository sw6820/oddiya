# ==========================================
# ODDIYA PROJECT - ENVIRONMENT CONFIGURATION
# ==========================================
# Copy this file to .env or .env.local for local development
# For production, use AWS Secrets Manager

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
DB_HOST=localhost                           # PostgreSQL host
DB_PORT=5432                                # PostgreSQL port
DB_NAME=oddiya                              # Database name
DB_USER=oddiya_user                         # Database user
DB_PASSWORD=oddiya_password_dev             # CHANGE IN PRODUCTION! Use AWS Secrets Manager

# ==========================================
# REDIS CONFIGURATION
# ==========================================
REDIS_HOST=localhost                        # Redis host
REDIS_PORT=6379                             # Redis port
REDIS_PASSWORD=                             # Optional Redis password
REDIS_CACHE_TTL=3600                        # Cache TTL in seconds (1 hour for LLM responses)

# ==========================================
# AWS INFRASTRUCTURE
# ==========================================
AWS_REGION=ap-northeast-2                   # Main AWS region (Seoul)
AWS_ACCESS_KEY_ID=                          # AWS credentials (for Bedrock, S3, SQS)
AWS_SECRET_ACCESS_KEY=                      # AWS secret key

# S3 Storage
S3_BUCKET=oddiya-storage-dev                # S3 bucket for photos/videos

# SQS & SNS (Video Pipeline)
SQS_QUEUE_URL=https://sqs.ap-northeast-2.amazonaws.com/YOUR_ACCOUNT/oddiya-video-jobs
SNS_TOPIC_ARN=arn:aws:sns:ap-northeast-2:YOUR_ACCOUNT:oddiya-notifications

# ==========================================
# LLM CONFIGURATION (LLM-Only Architecture)
# ==========================================
# AWS Bedrock - Claude Sonnet (Primary)
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-5-20250929-v1:0  # Claude Sonnet 4.5 (latest)
BEDROCK_REGION=us-east-1                    # Bedrock uses us-east-1
MOCK_MODE=true                              # Set to false for real Bedrock calls

# Alternative: Use older Claude 3.5 Sonnet
# BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0

# LLM Agent Service
LLM_AGENT_URL=http://llm-agent:8000         # Python FastAPI service URL

# LangSmith (Optional - for LLM tracing/monitoring)
LANGSMITH_API_KEY=                          # Optional: LangSmith API key
LANGSMITH_PROJECT=oddiya-dev                # LangSmith project name
LANGSMITH_API_URL=https://api.smith.langchain.com

# ==========================================
# AUTHENTICATION & AUTHORIZATION
# ==========================================
# OAuth 2.0 Providers (Auth Service)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_secret_here
OAUTH_REDIRECT_URI=http://localhost:3000/oauth/callback/google

# Apple Sign In (Optional)
APPLE_CLIENT_ID=com.oddiya.app
APPLE_TEAM_ID=YOUR_TEAM_ID
APPLE_KEY_ID=YOUR_KEY_ID
APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# JWT Configuration
JWT_ACCESS_TOKEN_VALIDITY=3600              # 1 hour (in seconds)
JWT_REFRESH_TOKEN_VALIDITY=1209600          # 14 days (in seconds)

# User Service URL (for Auth Service)
USER_SERVICE_URL=http://user-service:8082

# ==========================================
# EXTERNAL APIS
# ==========================================
# OpenWeatherMap (Weather data for travel planning)
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Kakao Local API (Optional - not used in LLM-only mode)
KAKAO_REST_API_KEY=your_kakao_rest_api_key_here
KAKAO_LOCAL_API_KEY=your_kakao_local_api_key_here

# ExchangeRate API (Priority 2 - not implemented yet)
EXCHANGERATE_API_KEY=your_exchangerate_api_key_here

# ==========================================
# APPLICATION SETTINGS
# ==========================================
ENVIRONMENT=development                     # development | staging | production
LOG_LEVEL=DEBUG                             # DEBUG | INFO | WARN | ERROR

# ==========================================
# TASK MASTER AI (Optional - for project management)
# ==========================================
# Only needed if using Task Master AI tool
ANTHROPIC_API_KEY=                          # Format: sk-ant-api03-...
PERPLEXITY_API_KEY=                         # Format: pplx-...
OPENAI_API_KEY=                             # Format: sk-proj-...
GOOGLE_API_KEY=                             # For Google Gemini models
GITHUB_API_KEY=                             # Format: ghp_... or github_pat_...

# ==========================================
# NOTES
# ==========================================
# 1. For local development: Copy this file to .env.local
# 2. For production: Use AWS Secrets Manager for sensitive values
# 3. Never commit .env or .env.local files to git
# 4. Update .gitignore to exclude: .env, .env.local, .env.bedrock
# 5. LLM-Only Architecture: No hardcoded travel data - all from Claude Sonnet
