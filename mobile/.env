# ===========================================
# Oddiya Mobile - Environment Configuration
# ===========================================
# This file contains DEFAULT values for all environments
# DO NOT put sensitive data here - use .env.local instead
#
# Load Priority:
# 1. .env.local (git-ignored, your personal overrides)
# 2. .env (this file, committed to git)
#
# To customize: Copy values to .env.local and modify

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_WEB_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Backend Configuration
# AWS EC2 Elastic IP (permanent IP, survives stop/start)
AWS_EC2_IP=43.202.149.120

# Environment Mode
# Options: 'local' (localhost) | 'aws' (EC2 backend)
BACKEND_ENV=local

# ===========================================
# Port Reference (Auto-configured)
# ===========================================
# Local Dev:  Auth=8082, Plan=8083, LLM=8000 (Metro uses 8081)
# AWS Prod:   Auth=8081, Plan=8083, LLM=8000 (no Metro)
