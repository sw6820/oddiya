# Google OAuth Configuration
# Get this from Google Cloud Console > APIs & Services > Credentials
# Android OAuth 2.0 Client ID (with SHA-1 registered)
GOOGLE_WEB_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Backend Configuration
# AWS EC2 Elastic IP (permanent IP address that survives stop/start)
AWS_EC2_IP=43.202.149.120

# Environment Mode
# Set to 'local' to use localhost, 'aws' to use EC2 backend
BACKEND_ENV=local

# Port Configuration (automatically handled based on BACKEND_ENV)
# Local: Auth=8082 (Metro uses 8081), Plan=8083, LLM=8000
# AWS: Auth=8081 (no Metro), Plan=8083, LLM=8000
